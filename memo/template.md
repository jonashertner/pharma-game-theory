# Roche — Strategic Memo: {{scenario_name}}

**To:** Roche Executive Committee
**From:** Strategic-Analysis Working Group (independent)
**Date:** {{date}}
**Re:** Path to a deal-acceptable architecture for the 2026 US pharma-pricing negotiation
**Snapshot:** {{snapshot_date}}
**Classification:** Internal — public-source synthesis

> This memo is generated by an independent multi-actor game-theoretic
> simulation (see `/Users/jonashertner/roche-game-theory/`). Every actor
> position cited traces to a public source in `data/actors/`. The
> recommendation reflects the model's analysis — it is not a Roche-internal
> document and includes no NDA-protected information.

---

## 1. Situation in one paragraph

{{situation_paragraph}}

## 2. Each-actor view (12-actor scan)

Each row is the position the model attributes to that actor based on the
sourced `data/actors/` profile. "Surplus" = the actor's modeled payoff
under the current scenario relative to its BATNA.

| Actor | Position summary | Surplus vs BATNA | Driving issue |
|---|---|---:|---|
{{actor_table_rows}}

## 3. Trade-space analysis

The 13 negotiation issues divide into three classes for this scenario:

**(a) Issues where the principal triple (Roche / US-exec / Swiss-FC) all win
together if moved correctly.** These are the engine of any deal.

{{class_a_issues}}

**(b) Issues with bilateral surplus — a deal between two principals at the
cost of one third party.** Use these as side-pay for the third party.

{{class_b_issues}}

**(c) Issues at red-line stress — moving these in any direction creates a
walk threat.** Treat as frozen; revisit only if the deal otherwise stalls.

{{class_c_issues}}

## 4. Recommended deal architecture

For each issue, the model's recommended setting under {{equilibrium_concept}}:

| Issue | Recommended value | Rationale | Acceptability test |
|---|---:|---|---|
{{deal_table_rows}}

**Equilibrium concept used:** {{equilibrium_concept}} — {{concept_rationale}}

## 5. Why each principal can accept this

Each principal's signing rationale, derived from their payoff calculation
under the recommended deal:

{{principal_signing_rationales}}

## 6. Sensitivity & risks

The recommendation is sensitive to the following parameter uncertainties:

{{sensitivity_block}}

**Walk-away risks** (per actor, ordered by surplus thinness):

{{walk_risks_block}}

## 7. Novel moves the model surfaces

From the open-collider-style domain-collision module:

{{novel_moves_block}}

## 8. Sequencing

The recommended deal requires this implementation order:

{{sequencing_block}}

## 9. Open questions for Roche Executive Committee

{{open_questions_block}}

---

## Appendix A: Model inputs

- **Actors profiled:** 12, with full sourced files in `data/actors/`.
- **Issues modeled:** 13, defined in `data/issues.yaml`.
- **Positions:** {{position_cell_count}} (actor, issue) cells in `data/positions.yaml`.
- **Equilibrium solvers run:** Nash bargaining, Kalai-Smorodinsky, Shapley, core check.
- **Calibration test:** the May 2026 status quo (Genentech MFN deal + Swiss
  framework + IRA Round 3 selections) is feasible under the model. ✓

## Appendix B: Sourcing

All factual claims in this memo are sourced. See the inline citations
above and the dated reference list at `data/actors/<actor>.md` for each
named position. Snapshot date: {{snapshot_date}}.

## Appendix C: Method note

The model is described in `model/README.md`. It is a transparent
linear-with-red-line-kinks payoff structure. It is *not* a black-box
prediction; it is a *structured argument*. Where the model recommends
something counterintuitive, that's a deliberate output of writing down
each actor's interests precisely — not a hidden assumption.
